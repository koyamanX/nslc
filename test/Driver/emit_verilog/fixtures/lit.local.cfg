# SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
#
# test/Driver/emit_verilog/fixtures/lit.local.cfg — fixture-source
# directory exclusion. The `.nsl` files here are shared *source
# inputs* for the sibling `.test` lit fixtures (referenced via
# `%S/fixtures/<name>.nsl`); they MUST NOT be auto-discovered as
# tests themselves.

config.suffixes = ()
config.excludes = ['fixtures']
